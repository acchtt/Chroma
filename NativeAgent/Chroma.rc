#include "resource.h"

// Keep the native executable and system-tray icon synchronized with the
// WinUI application branding instead of maintaining a separate icon copy.
IDI_CHROMA ICON "..\\Chroma.WinUI\\Assets\\Chroma.ico"
